const config = {
  // App configuration
  appName: "Meal Planner",
  appDescription: "AI-powered personalized meal planning app",
  domainName: "localhost:3000",

  // Color configuration for NextAuth theme
  colors: {
    main: "#10b981", // emerald-500 - matches health/food theme
  },

  // Email configuration (for future use)
  resend: {
    fromNoReply: "noreply@mealplanner.com",
    supportEmail: "support@mealplanner.com",
  },
};

export default config;
